import { Ealge } from './eagle.js';
import { gAssetsManager } from './main.js';

export class Flotte {
    constructor(screenWidht, screenHeight, score) {
        this.score = score;
        this.row = 100000;
        this.ealges = new Array(this.row);
        
    }
}